@model Forum.Entity.Models.Question

@{
    ViewData["Title"] = "Soru Detayı";
}

<div class="container mt-4">
    <div class="card mb-4">
        <div class="card-header">
            <h2>@Model.Title</h2>
        </div>
        <div class="card-body">
            <p>@Model.Description</p>
            <small class="text-muted">Tarih: @Model.CreatedAt</small>
        </div>
    </div>

    <h4>Yorumlar / Cevaplar</h4>

    <div id="comments-list">
        @if (Model.Comments != null && Model.Comments.Any())
        {
            @foreach (var comment in Model.Comments)
            {
                <div class="card mb-2 shadow-sm" id="comment-@comment.Id">
                    <div class="card-body py-2">
                        <div class="d-flex justify-content-between align-items-center">

                            <div>
                                <p class="mb-1 fw-bold text-dark">@comment.Content</p>
                                <small class="text-muted">
                                    <i class="fas fa-user-edit"></i> @(comment.User?.Name ?? "Misafir")
                                    |
                                    <i class="far fa-clock"></i> @comment.CreatedAt.ToString("dd.MM.yyyy HH:mm")
                                </small>
                            </div>

                            @if (User.Identity.IsAuthenticated)
                            {
                                var currentUserId = int.Parse(User.FindFirst("UserId")?.Value ?? "0");

                                // Yorum sahibi veya Admin ise görsün
                                if (currentUserId == comment.UserId || User.IsInRole("Admin"))
                                {
                                    // response.commentId kısmına dikkat et!
                                    <a href="/Home/DeleteComment/${response.commentId}"
                                       class="btn btn-outline-danger btn-sm"
                                       onclick="return confirm('Silmek istediğinize emin misiniz?');">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                }
                            }

                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div id="no-comment-warning" class="alert alert-light">Henüz yorum yapılmamış. İlk cevabı sen yaz!</div>
        }
    </div>

    <hr />

    <div class="card bg-light">
        <div class="card-body">
            <h5 class="card-title">Cevap Yaz</h5>
            <form id="commentForm">
                <div class="form-group mb-3">
                    <textarea id="txtComment" class="form-control" rows="3" placeholder="Yorumunuzu buraya yazın..."></textarea>
                </div>
                <button type="button" id="btnSend" class="btn btn-primary">
                    <i class="fas fa-paper-plane"></i> Gönder
                </button>
            </form>
        </div>
    </div>

    <hr />

</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<script>
    $(document).ready(function () {

        $("#btnSend").click(function () {
            // 1. Textarea'daki yazıyı al
            var yorumIcerigi = $("#txtComment").val();
            var soruId = @Model.Id; // Sorunun ID'sini al

            if (yorumIcerigi === "") {
                alert("Lütfen bir şeyler yazın!");
                return;
            }

            // 2. AJAX İsteği Gönder
            $.ajax({
                url: '/Home/AddComment', // Controller'daki metoda git
                type: 'POST',
                data: { content: yorumIcerigi, questionId: soruId },
                success: function (response) {
                    if (response.success) {
                        // 3. Başarılı olursa sayfayı yenilemeden yorumu ekrana ekle
                        var yeniYorumHTML = `
                            <div class="alert alert-success">
                                ${yorumIcerigi}
                                <br/>
                                <small>Az önce</small>
                            </div>`;

                        $("#comments-list").append(yeniYorumHTML);
                        $("#txtComment").val(""); // Kutuyu temizle
                        alert("Yorumunuz eklendi!");
                    } else {
                        alert("Hata: " + response.message);
                    }
                },
                error: function () {
                    alert("Bir hata oluştu. Giriş yaptığınızdan emin olun.");
                }
            });
        });

    });
</script>
@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
        $(document).ready(function () {

            // Tıklama olayını sadece BİR KERE tanımlıyoruz
            $("#btnSend").off().click(function () {

                var yorumIcerigi = $("#txtComment").val();
                var soruId = @Model.Id;

                if (yorumIcerigi.trim() === "") {
                    alert("Lütfen bir yorum yazın!");
                    return;
                }

                var $btn = $(this);
                $btn.prop("disabled", true).text("Gönderiliyor...");

                $.ajax({
                    url: '/Home/AddComment',
                    type: 'POST',
                    data: { content: yorumIcerigi, questionId: soruId },
                    success: function (response) {
                        if (response.success) {

                            var now = new Date();
                            var zaman = now.toLocaleDateString() + " " + now.toLocaleTimeString().substring(0,5);

                            // Sadece BEYAZ KART tasarımını kullanıyoruz
                            var yeniYorumHTML = `
                                <div class="card mb-2 shadow-sm border-start border-success border-3">
                                    <div class="card-body py-2">
                                        <div class="d-flex justify-content-between align-items-center">
                                            <div>
                                                <p class="mb-1 fw-bold text-dark">${yorumIcerigi}</p>
                                                <small class="text-muted">
                                                    <i class="fas fa-user-check"></i> Ben (Az önce)
                                                    |
                                                    <i class="far fa-clock"></i> ${zaman}
                                                </small>
                                            </div>

                                            <a href="/Home/DeleteComment/${response.commentId}"
                                               class="btn btn-outline-danger btn-sm"
                                               onclick="return confirm('Silmek istediğinize emin misiniz?');">
                                                <i class="fas fa-trash-alt"></i>
                                            </a>
                                        </div>
                                    </div>
                                </div>`;

                            $("#no-comment-warning").remove();
                            $("#comments-list").append(yeniYorumHTML);
                            $("#txtComment").val("");

                            // Popup uyarısını da kaldırabiliriz, zaten ekranda beliriyor
                            // alert("Yorumunuz eklendi!");

                        } else {
                            alert("Hata: " + response.message);
                        }
                    },
                    error: function () {
                        alert("Sunucuyla iletişim kurulamadı.");
                    },
                    complete: function () {
                        $btn.prop("disabled", false).html('<i class="fas fa-paper-plane"></i> Gönder');
                    }
                });
            });

        });
    </script>
}